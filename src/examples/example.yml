description: >
  Sample example description.
# Provide a use-case based example for using this orb.
# Everything in the `usage` section will be displayed in the orb registry.
# Comments are not retained.
usage:
  version: 2.1
  orbs:
    ms-teams-notify: rosswhite/ms-teams-notify@0.0.1
  workflows:
    use-my-orb:
      jobs:
        - ms-teams-notify/notify:
            DEPLOYED_ENV: 'PROD'
            WEBHOOK_URL: $MS_TEAMS_WORKFLOW_URL
